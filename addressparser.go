package addressparser

type AddressParser struct {}


func (parser *AddressParser) AddressParser(address string) *Address {
	//search := SearchVariant{}
	//variants := search.Parse(address)
	//parsedVariants := variants[0].
	return &Address{}
}